---
// The old complex script and hardcoded examples are removed.
// We now import and use the InteractiveCodeDemo React component.
import InteractiveCodeDemo from './InteractiveCodeDemo.tsx';
---

<section class="bg-slate-900 text-white py-20">
  <div class="container mx-auto px-6 text-center">
    <h2 class="text-4xl font-bold mb-4">Simple, powerful, and intuitive</h2>
    <p class="text-gray-400 mb-12 text-lg">Just a few lines of code to get your API up and running.</p>
    
    {/* The InteractiveCodeDemo component handles tabs, code display, input fields, and mock responses */}
    <InteractiveCodeDemo client:visible />
  </div>
</section>

<style>
  /* Minimal styles for the section itself. 
     The InteractiveCodeDemo component will manage its own internal styling. 
     Ensure bg-slate-900 is defined if not globally available, or adjust as needed.
  */
  .bg-slate-900 {
     /* background-color: #0f172a; */ /* Example if not globally set by Tailwind */
  }

  /* Custom scrollbar styles from the original component can be kept if desired for general x-overflowing content,
     or moved into the React component if specific to its scroll areas.
     For now, keeping them here as they might apply to other elements in this section if any.
  */
  .overflow-x-auto::-webkit-scrollbar {
    height: 8px;
  }
  .overflow-x-auto::-webkit-scrollbar-track {
    background: #1f2937; /* scrollbar track */
    border-radius: 4px;
  }
  .overflow-x-auto::-webkit-scrollbar-thumb {
    background: #374151; /* scrollbar thumb */
    border-radius: 4px;
  }
  .overflow-x-auto::-webkit-scrollbar-thumb:hover {
    background: #4b5563; /* scrollbar thumb on hover */
  }
</style>
